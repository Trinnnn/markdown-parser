import static org.junit.Assert.*;
import org.junit.*;

public class MarkdownParseTest {
    @Test
    public void addition() { // method with no return, only for junit test
        
        // if 1+1 = 2, return true. assertEquals(expected, actual);
        assertEquals(2, 1 + 1);
    }
}
